# Adam 算法复习回顾

## 1. Adam 要解决什么问题

普通梯度下降对所有参数使用相同的学习率：

```text
θₜ = θₜ₋₁ - ηgₜ
```

其中：

- `θₜ`：第 `t` 步的模型参数；
- `η`：学习率；
- `gₜ`：第 `t` 步的梯度。

这种方法有两个常见问题：

1. 单次梯度可能来回震荡，更新方向不够稳定；
2. 不同参数的梯度大小不同，却共用同一个更新步长。

Adam 同时吸收了两种思想：

- **Momentum**：记录梯度的平均方向，使更新方向更平滑；
- **RMSProp**：记录梯度平方的平均值，为每个参数调整实际步长。

一句话概括：

> Adam 使用一阶矩估计更新方向，使用二阶矩估计调整每个参数的更新幅度。

---

## 2. Adam 的完整执行步骤

### 第 1 步：计算纯梯度

```text
gₜ = ∇θ J(θₜ₋₁)
```

含义是：计算损失函数 `J` 在当前参数 `θₜ₋₁` 处的梯度。

梯度 `gₜ` 告诉我们两个信息：

- 正负号表示参数应该向哪个方向调整；
- 绝对值表示损失函数在当前位置有多陡。

因为梯度指向损失增长最快的方向，所以参数更新时要沿梯度的反方向移动。

这里强调“纯梯度（不含 L2）”，通常是为了后续与 AdamW 对比。在 AdamW 中，权重衰减与梯度更新是分开处理的。

### 第 2 步：更新一阶矩估计

```text
mₜ = β₁mₜ₋₁ + (1 - β₁)gₜ
```

`mₜ` 是历史梯度的指数移动平均，可以理解为“最近一段时间内，梯度总体想往哪个方向走”。

- `mₜ₋₁`：过去梯度方向的累计信息；
- `gₜ`：当前梯度；
- `β₁`：保留多少历史信息，通常取 `0.9`。

当 `β₁ = 0.9` 时：

```text
mₜ = 0.9mₜ₋₁ + 0.1gₜ
```

也就是说，新的一阶矩中约有 90% 来自历史，10% 来自当前梯度。这样可以减少单次梯度波动造成的震荡。

### 第 3 步：更新二阶矩估计

```text
vₜ = β₂vₜ₋₁ + (1 - β₂)(gₜ ⊙ gₜ)
```

也可以简写为：

```text
vₜ = β₂vₜ₋₁ + (1 - β₂)gₜ²
```

这里的 `gₜ²` 表示**逐元素平方**，不是向量点积。符号 `⊙` 表示逐元素相乘。

`vₜ` 记录历史梯度平方的指数移动平均，可以理解为“最近的梯度通常有多大”。`β₂` 通常取 `0.999`。

二阶矩会影响每个参数的实际学习率：

- 某个参数的梯度一直很大，`vₜ` 就大，分母变大，更新步长被压小；
- 某个参数的梯度一直很小，`vₜ` 就小，分母较小，更新步长相对变大。

因此，Adam 能够为不同参数自动调整更新幅度。

### 第 4 步：进行偏差修正

Adam 通常将一阶矩和二阶矩初始化为零：

```text
m₀ = 0
v₀ = 0
```

这会导致训练刚开始时，`mₜ` 和 `vₜ` 都偏向零。因此需要进行偏差修正：

```text
m̂ₜ = mₜ / (1 - β₁ᵗ)
v̂ₜ = vₜ / (1 - β₂ᵗ)
```

其中：

- `m̂ₜ` 读作“m hat t”，表示修正后的一阶矩；
- `v̂ₜ` 读作“v hat t”，表示修正后的二阶矩。

以第 1 步为例，假设 `m₀ = 0`、`β₁ = 0.9`：

```text
m₁ = 0.9 × 0 + 0.1g₁ = 0.1g₁
```

如果直接使用 `m₁`，它只有真实梯度的 10%。修正后：

```text
m̂₁ = m₁ / (1 - 0.9¹)
    = 0.1g₁ / 0.1
    = g₁
```

所以偏差修正主要解决训练初期估计值过小的问题。随着 `t` 增大，`β₁ᵗ` 和 `β₂ᵗ` 会越来越接近零，修正效果也会逐渐减弱。

### 第 5 步：更新参数

```text
θₜ = θₜ₋₁ - η × m̂ₜ / (√v̂ₜ + ε)
```

其中：

- `η`：基础学习率，Adam 常用默认值为 `0.001`；
- `m̂ₜ`：修正后的一阶矩，主要决定更新方向；
- `√v̂ₜ`：修正后的梯度尺度，主要调节更新幅度；
- `ε`：极小正数，用于防止分母为零，常取 `10⁻⁸`。

必须注意，分母中有平方根 `√v̂ₜ`。因为 `v̂ₜ` 保存的是梯度的平方，开平方后才与梯度处于相同的量级。

---

## 3. 一个单参数数值例子

假设：

```text
θ₀ = 1.0
η  = 0.001
β₁ = 0.9
β₂ = 0.999
ε  = 10⁻⁸
g₁ = 0.2
m₀ = 0
v₀ = 0
```

### 计算一阶矩

```text
m₁ = 0.9 × 0 + 0.1 × 0.2
   = 0.02
```

### 计算二阶矩

```text
v₁ = 0.999 × 0 + 0.001 × 0.2²
   = 0.001 × 0.04
   = 0.00004
```

### 偏差修正

```text
m̂₁ = 0.02 / (1 - 0.9¹)
    = 0.2

v̂₁ = 0.00004 / (1 - 0.999¹)
    = 0.04
```

### 更新参数

```text
θ₁ = 1.0 - 0.001 × 0.2 / (√0.04 + 10⁻⁸)
   ≈ 1.0 - 0.001 × 0.2 / 0.2
   ≈ 0.999
```

可以看到，第 1 步中经过偏差修正后，Adam 的更新量大约为一个学习率 `0.001`。

---

## 4. 如何记忆 Adam

可以把 Adam 的五个步骤记成：

```text
算梯度 → 看方向 → 看大小 → 修正初始偏差 → 更新参数
```

对应关系是：

```text
gₜ  ：当前梯度
mₜ  ：梯度平均值，描述历史方向
vₜ  ：梯度平方平均值，描述历史大小
m̂ₜ ：修正后的一阶矩
v̂ₜ ：修正后的二阶矩
θₜ  ：更新后的参数
```

最终更新公式可以理解为：

```text
参数更新量 = 基础学习率 × 平滑后的方向 / 历史梯度尺度
```

---

## 5. 容易写错的地方

1. **偏差修正前后不能使用同一个符号。**

   修正前是 `mₜ、vₜ`，修正后应写成 `m̂ₜ、v̂ₜ`。

2. **参数更新的分母中必须有平方根。**

   正确形式是 `√v̂ₜ + ε`，不是 `v̂ₜ + ε`。

3. **梯度平方是逐元素平方。**

   `gₜ²` 不是两个梯度向量做点积。

4. **参数更新使用的是上一步参数。**

   左边是 `θₜ`，右边是 `θₜ₋₁`。

5. **ε 不负责控制学习率。**

   它的主要作用是提高数值稳定性，防止分母为零或过小。

---

## 6. PyTorch 中的对应写法

```python
optimizer = torch.optim.Adam(
    model.parameters(),
    lr=1e-3,
    betas=(0.9, 0.999),
    eps=1e-8,
)

for x, y in dataloader:
    optimizer.zero_grad()
    prediction = model(x)
    loss = loss_fn(prediction, y)
    loss.backward()  # 计算 gₜ
    optimizer.step() # 更新 mₜ、vₜ并更新 θₜ
```

`optimizer.step()` 内部完成了一阶矩更新、二阶矩更新、偏差修正和参数更新。

## 7. 一句话总结

> Adam 先用梯度的指数移动平均确定稳定的更新方向，再用梯度平方的指数移动平均调整每个参数的更新幅度，最后通过偏差修正解决训练初期估计偏小的问题。
